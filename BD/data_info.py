# 7.SELECT user_id, name, sex, old, score FROM users
# 8.SELECT * FROM users WHERE sex  = 2
# 9.SELECT * FROM users WHERE score  < 1000
# 10.SELECT * FROM users WHERE score > 1000 ORDER BY old DESC
# 11.
# 12.
